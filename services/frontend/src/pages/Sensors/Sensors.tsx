import React from 'react';
import { Typography } from '@mui/material';

const Sensors: React.FC = () => (
  <Typography variant="h4">Gestion des Capteurs IoT</Typography>
);

export default Sensors;