import React from 'react';
import { Typography } from '@mui/material';

const Settings: React.FC = () => (
  <Typography variant="h4">Paramètres et Configuration</Typography>
);

export default Settings;